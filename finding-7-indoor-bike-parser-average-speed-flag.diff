diff --git a/app/src/main/java/com/example/ergometerapp/ftms/IndoorBikeParser.kt b/app/src/main/java/com/example/ergometerapp/ftms/IndoorBikeParser.kt
index faae8aa..81956c2 100644
--- a/app/src/main/java/com/example/ergometerapp/ftms/IndoorBikeParser.kt
+++ b/app/src/main/java/com/example/ergometerapp/ftms/IndoorBikeParser.kt
@@ -46,9 +46,9 @@ fun parseIndoorBikeData(bytes: ByteArray): IndoorBikeData {
         // Instantaneous Speed is always present per spec.
         val instantSpeed = u16() / 100.0
 
-        // Some devices (e.g., Tunturi E80) send Average Speed even with flag(0) cleared.
+        // Average Speed is optional and present only when flag(0) is set.

         // Cadence fields are optional; missing fields are preserved as null.
         val instantCadence =
